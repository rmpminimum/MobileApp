package com.nano.min.di

import org.koin.dsl.module

val interactorModule = module {
    // Add interactors/use cases here as the app grows
    // For now, we'll use AuthService directly in ViewModels
    // In a more complex app, you would create separate use case classes
}
